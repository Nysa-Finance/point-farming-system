# Pinned to the versions the tracker was built and verified against. anchorpy pulls
# anchorpy-core, borsh-construct and construct itself; solana pulls solders, httpx and
# websockets. Bump deliberately — a borsh/construct change silently breaks account decoding.
anchorpy==0.21.0
solana==0.36.6
solders==0.26.0
base58==2.1.1
